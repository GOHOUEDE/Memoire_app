<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recommandation Client</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .button-group button {
            margin-right: 10px;
            font-size: 16px;
            transition: box-shadow 0.3s; /* Transition pour l'ombre */
        }

        .button-group .accepted {
            box-shadow: 0px 0px 8px 0px rgba(0,0,0,0.75); /* Ombre pour le bouton "Accepté" sélectionné */
        }
    </style>
</head>
<body>
    <div class="container">
        <ul class="list-unstyled mb-1-9">
            <h2>Recommandation :</h2>
            <li style="color: {% if Nature == 'Bon' %}green{% else %}red{% endif %}" class="mb-2 mb-xl-3 display-28">
                {{ recom }}
            </li>
            <div class="button-group">
                <button type="button" class="btn btn-success {% if Nature == 'Bon' and note > 75 %} accepted {% endif %}">Accepté</button>
                <button type="button" class="btn btn-danger {% if Nature == 'Mauvais' %} accepted {% endif %}">Rejeté</button>
                <button type="button" class="btn btn-warning">À étudier</button>
            </div>
        </ul>
    </div>
</body>
</html>
